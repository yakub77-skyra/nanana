# ---------------------------------------------------------------
# BUILD STAMP — M009 GUARD
# Bump this string on EVERY commit from now on.
# The run log prints it, so we can always prove WHICH code executed.
# If a video looks wrong but the log shows the new build → code bug.
# If the log shows an old build → deployment miss (unpushed files).
# ---------------------------------------------------------------
BUILD = "P6.3c"